name: onboard-dismap-command
on:
  repository_dispatch:
    types: [onboard-dismap-command]
jobs:
  list:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
    steps:
      - name: Create comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.event.client_payload.github.payload.repository.full_name }}
          issue-number: ${{ github.event.client_payload.github.payload.issue.number }}
          # TODO: Figure out if lines are wrapped as given or not
          body: |
            Welcome :wave: to the [DisMAP Team](https://github.com/nmfs-ost/DisMAP),
            below you will find a list of onboarding tasks for both the onboardee and for the Project Lead.
            Please mark off tasks once they are accomplished to ease the communication chain and decrease duplication. 
            Feel free reach out on the project lead, leave a comment on this issue, or open a new issue if you want 
            to chat about anything.

            ## Onboardee
              - [ ] Request access to [DisMAWG google folder](https://drive.google.com/drive/folders/15KC3DDLVyNGDfZ9ChpjaHUuesYhF6ai0)

            ## Project Lead
              - [ ] Add to monthly DisMAWG meetings
              - [ ] Add to biweekly development team meetings
              - [ ] Add as collaborator to [DisMAP GitHub Repository](https://github.com/nmfs-ost/DisMAP)